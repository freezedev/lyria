define('spec/componentmanager', ['lyria/component/manager'], function(ComponentManager) {
  'use strict';
  
  describe('lyria/component/manager', function() {
    
    it('is an object', function() {
      expect(ComponentManager).to.be.a('object');
    });
    
  });
  
});
